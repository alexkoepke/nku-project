<div class="row col-sm-6">
  <%= form_for @join do |f| %>
    <% if @join.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@join.errors.count, "error") %> prohibited
          you from joining:</h2>
        <ul>
        <% @join.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= f.label 'What are you playing today?' %>
      <%= f.text_field :game, class: 'form-control' %>
    </div>

    

    <%= f.submit "Create", class: 'btn btn-primary' %>
  <% end %>
</div>